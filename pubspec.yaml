name: alam_bayanat_laf_el_moharakat
description: تطبيق عالم بيانات لف المحركات - قاعدة بيانات وفهرس لبيانات لف المحركات الكهربائية.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
